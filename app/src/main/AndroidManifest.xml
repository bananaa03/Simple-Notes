<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UIdoan"
        tools:targetApi="31">
        <activity android:name=".day_main" />
        <activity android:name=".night_main" />
        <activity android:name=".setting_day" />
        <activity android:name=".setting_night" />
        <activity android:name=".note_day" />
        <activity android:name=".log_in" />
        <activity android:name=".registerpage" />
        <activity android:name=".user_day" />
        <activity android:name=".user_night" />
        <activity android:name=".changePassword" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>